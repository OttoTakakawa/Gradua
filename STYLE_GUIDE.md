# Within 项目前端样式规范

> 本文档供 AI 读取，用于在修改 HTML/CSS 时节省 token、保持一致性。
> 最后更新：2026-05-07

---

## 1. 项目结构

```
Gradua/
├── shared.css          ← 全局样式（导航、页脚、过渡、通用组件）
├── about/index.html    ← 项目介绍页（含页面级 <style>）
├── design/index.html   ← 核心设计页（含页面级 <style>）
├── research/index.html ← 问题研究页（含页面级 <style>）
├── process/index.html  ← 制作过程页（含页面级 <style>）
├── chapters/index.html ← 章节总览页
├── chapters/chapter-1~4/index.html ← 四个章节详情页
├── thanks/index.html   ← 致谢页
├── images/             ← 所有图片资源
└── STYLE_GUIDE.md      ← 本文档
```

**关键规则**：每个 HTML 文件在 `<style id="page-styles">` 标签内定义页面专属样式，shared.css 定义全局样式。修改时先确认属性属于全局还是页面级。

---

## 2. 设计语言

- **风格**：冷灰蓝系极简展板，苹果式"呼吸感"
- **禁止**：渐变边框、鲜艳色块、粗重描边、实验性布局
- **核心**：大量留白、居中对齐、低饱和度、统一卡片质感

---

## 3. CSS 变量（shared.css :root）

```css
--text-main: #536171;    /* 正文主色 */
--text-dark: #243241;    /* 标题深色 */
--text-soft: #7f90a2;    /* 辅助说明文字 */
--blue: #6e879f;         /* 通用蓝 */
--cyan: #8baebe;         /* 青色 */
--cyan-deep: #5d8693;    /* 深青，用于标签和强调 */
--purple: #8f8eb0;       /* 紫色，用于第二层级 */
--warm: #bda583;         /* 暖色，用于第三层级 */
--border: rgba(118,136,158,0.14);
--shadow: 0 18px 48px rgba(86,105,128,0.08);
--shadow-hover: 0 22px 56px rgba(86,105,128,0.12);
```

**规则**：新增颜色时优先复用以上变量。同义色值（如 #5a7a8c ≈ var(--cyan-deep)）应统一到变量。

---

## 4. 字体

```css
font-family: "PingFang SC", "Microsoft YaHei", "Noto Sans SC", sans-serif;
```

- **PingFang SC**（苹方）已是首选字体，macOS/iOS 原生支持
- Windows 上会回退到微软雅黑，效果可接受
- 无需额外引入字体文件

---

## 5. 字号层级（应统一到以下规范）

| 层级 | 用途 | 推荐值 | 当前混乱值 |
|------|------|--------|-----------|
| h1 | 页面主标题 | clamp(42px, 5.6vw, 72px) | 已统一 |
| h2 | 章节标题 | clamp(34px, 4vw, 50px) | 34px/38px/40px 混用 |
| h3 | 卡片/模块标题 | 24px | 19px/22px/24px/26px/28px 混用 |
| 正文 | 段落文字 | 15px | 14px/15px/16px 混用 |
| 辅助 | 说明/注释 | 13px | 12px/13px/14px 混用 |
| 微标 | 标签/角标 | 11px | 10px/11px/12px 混用 |

**当前问题**：h3 出现了 7 种不同字号（19/22/24/26/28/30/32px），是最混乱的层级。

---

## 6. 行高规范

| 用途 | 推荐值 | 当前混乱值 |
|------|--------|-----------|
| 标题 | 1.2 | 1.02~1.35 共 10 种 |
| 正文 | 1.9 | 1.7~2.05 共 8 种 |
| 紧凑文字 | 1.6 | 1.5~1.85 共 6 种 |

**当前问题**：line-height 共出现 32 种不同值，是全项目最混乱的属性。

---

## 7. 间距规范

### 章节间距
| 用途 | 推荐值 | 当前值 |
|------|--------|--------|
| 章节之间 | 104~136px | 64px~136px 不等 |
| 标题到内容 | 22~28px | 10px~34px 不等 |

### 卡片间距
| 用途 | 推荐值 | 当前值 |
|------|--------|--------|
| 卡片 gap | 18px | 8px~44px 共 14 种 |
| 卡片 padding | 24px | 18px~36px 共 20+ 种 |
| 卡片 margin-top | 18px | 6px~34px 共 12 种 |

**当前问题**：gap 共 19 种值、padding 共 40+ 种组合，是最需要收敛的属性。

---

## 8. 颜色混乱度

当前硬编码颜色值（非变量）共 **40+ 种**，其中：
- 灰蓝系同义色至少 15 种（#5a7a8c / #688196 / #6d7f92 / #7b8ea1 / #7f93a3 等本质相同）
- 应统一到 `var(--text-soft)` / `var(--cyan-deep)` / `var(--text-main)` 三档

---

## 9. 统一策略建议

### 第一步：收敛字号到 6 档
```css
--fs-hero: clamp(42px, 5.6vw, 72px);
--fs-h2: clamp(34px, 4vw, 50px);
--fs-h3: 24px;
--fs-body: 15px;
--fs-small: 13px;
--fs-tiny: 11px;
```

### 第二步：收敛行高到 3 档
```css
--lh-heading: 1.2;
--lh-body: 1.9;
--lh-compact: 1.6;
```

### 第三步：收敛间距到 6 档
```css
--sp-xs: 8px;
--sp-s: 12px;
--sp-m: 18px;
--sp-l: 28px;
--sp-xl: 52px;
--sp-2xl: 104px;
```

### 第四步：颜色全部走变量
新增变量替代所有硬编码灰蓝色值。

---

## 10. 组件复用清单

以下组件已在 shared.css 中定义，各页面不应重复定义：

| 类名 | 用途 |
|------|------|
| `.wrap` | 内容容器，居中 + 上下 padding |
| `.nav` | 顶部导航栏 |
| `.eyebrow` | 章节小标（当前已 display:none） |
| `.card` | 通用白底卡片 |
| `.tag` | 胶囊标签 |
| `.btn-primary` / `.btn-secondary` | 按钮 |
| `.reveal-item` | 滚动渐入动画 |
| `.prev-arm-hint` / `.next-arm-hint` | 滚轮翻页提示 |

---

## 11. 修改规则

1. **先读 shared.css**：确认属性是否已有全局定义
2. **用变量不用硬编码**：颜色用 var(--xxx)，间距用语义化变量
3. **不改 archive/**：archive 目录是旧版参考，不修改
4. **移动端适配**：所有页面都有 `@media (max-width: 860px)` 和 `@media (max-width: 560px)` 断点
5. **居中优先**：所有内容模块必须水平居中，复杂结构用 Grid + margin: 0 auto
6. **间距只大不小**：用户偏好"呼吸感"，间距宁大勿小
7. **不要加框**：用户明确不喜欢外框、描边、背景框，除非是标准卡片
8. **不要加注释**：代码中不写注释

---

## 12. 文件修改优先级

修改样式时，按以下顺序选择位置：
1. `shared.css` — 全局组件、变量、通用样式
2. 页面 `<style id="page-styles">` — 仅本页面使用的样式
3. 行内 style — 仅用于一次性微调，尽量避免

---

## 13. 已知用户偏好

- 讨厌"拥挤"，要求间距宁大勿小
- 讨厌绿色/鲜艳色，偏好冷灰蓝
- 讨厌外框/描边/背景框，偏好无框白卡
- 讨厌实验性布局，偏好传统流程图/展板
- 标题和内容之间需要大间距（64px+）
- 所有信息必须居中
- 字号要够大，行高要够高
