# 2026-05-05 修改记录

## 13:33 核心设计页更新
- 重构 `design/index.html` 的第四章页面结构，将内容收束为“协作机制图示 + 空间布置图示”两大核心区块。
- 删除了独立的“角色分配图示”章节，避免与跨媒介协作机制主图重复叙述。
- 将两张角色参考图短暂并入机制区后再次移除，最终保留“大机制图”为主视觉说明中心。

## 13:33 协作机制交互整理
- 将跨媒介协作机制图右侧说明区收敛为“当前名词解释”卡片，不再堆叠阶段列表。
- 为角色分配阶段补充了节点联动预览：
  - 悬浮 `探索者 / 引导者 / 回应者` 时，右侧显示对应人物参考图。
  - 悬浮 `非对称性 + 互补依赖` 时，右侧显示三角色协作关系图。
- 保留机制图的放大查看与节点锁定逻辑，用于桌面端讲解演示。

## 13:33 空间布置模块调整
- 将“空间布置图示”改为左侧原始图示、右侧三维模型预览的双栏结构。
- 删除顶部筛选按钮，减少视觉噪声，保留整体对齐后的展示关系。
- 关闭左侧空间图示的 hover 高亮，让其作为静态参考图存在。
- 调整双栏卡片为同高对齐，保证桌面端展示更规整。

## 13:33 3D 模型测试链路
- 接入 `model-viewer` 用于网页内展示可旋转 `.glb` 模型。
- 新建测试模型 `models/space-layout.glb`，使用基础 box 结构验证拖拽旋转、缩放与本地预览链路。
- 确认 `file://` 直接打开时模型显示不稳定，因此改用本地静态服务器方式进行预览。

## 13:33 当前状态
- `design/index.html` 已形成更清晰的“主机制图 + 空间模型展示”结构。
- 测试模型可在本地服务器环境中正常显示，后续可直接替换为正式空间模型文件。
- 当前版本适合提交为一版阶段性更新，供后续继续替换正式素材与精修版式。
